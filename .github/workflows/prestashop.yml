name: phpClaw PrestaShop

on:
  push:
    branches: [0.x]
    paths:
      - 'packages/prestashop/**'
      - '!packages/prestashop/**.md'
      - '.github/workflows/prestashop.yml'
      - '.github/workflows/_adapter-quality.yml'
  pull_request:
    paths:
      - 'packages/prestashop/**'
      - '!packages/prestashop/**.md'
      - '.github/workflows/prestashop.yml'
      - '.github/workflows/_adapter-quality.yml'


concurrency:
  group: prestashop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    uses: ./.github/workflows/_adapter-quality.yml
    with:
      adapter: prestashop
      is_cms: true
      php_versions: '["8.1","8.2","8.3","8.4"]'

  test:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3', '8.4']

    defaults:
      run:
        working-directory: packages/prestashop

    steps:
      - uses: actions/checkout@v7

      - name: Set up PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: pdo, pdo_sqlite, json
          coverage: xdebug

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction --ignore-platform-req=php

      - name: Run tests with coverage
        run: composer test:coverage

      - name: Check coverage threshold
        continue-on-error: true
        run: |
          php -r "
            \$xml = simplexml_load_file('coverage/clover.xml');
            \$metrics = \$xml->project->metrics;
            \$covered = (int)\$metrics['coveredstatements'];
            \$total   = (int)\$metrics['statements'];
            if (\$total === 0) { echo 'No statements found'; exit(0); }
            \$pct = (\$covered / \$total) * 100;
            echo number_format(\$pct, 1) . '% coverage' . PHP_EOL;
            if (\$pct < 80) { echo 'Coverage below 80%'; exit(1); }
          "
